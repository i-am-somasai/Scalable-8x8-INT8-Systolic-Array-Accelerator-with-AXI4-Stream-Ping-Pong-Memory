You are acting as a senior RTL/VLSI architect, SystemVerilog designer, hardware verification engineer, computer-architecture engineer, and ASIC implementation engineer.

Your task is to DESIGN AND WRITE THE COMPLETE CODEBASE for the following project:

============================================================
PROJECT TITLE
============================================================

"Scalable 8×8 INT8 Systolic Array Accelerator with AXI4-Stream & Ping-Pong Memory"

The baseline architecture is an 8×8 systolic array containing 64 processing elements (PEs), designed for INT8 matrix multiplication.

The project must be developed as a serious undergraduate digital-VLSI/RTL project suitable for:
- Digital VLSI interviews
- RTL design interviews
- ASIC design interviews
- FPGA/hardware accelerator roles
- Computer architecture discussions
- Resume/project portfolio use

The goal is NOT to create a simple educational matrix multiplier.

The goal is to create a clean, parameterized, modular, verifiable hardware accelerator with:
- INT8 arithmetic
- 8×8 systolic architecture
- 64 MAC processing elements
- configurable/scalable array dimensions
- AXI4-Stream input/output
- ping-pong/double buffering
- controller/FSM
- robust SystemVerilog verification
- Python golden reference model
- synthesis-ready RTL
- ASIC-flow-ready structure
- detailed documentation
- performance/resource analysis infrastructure

============================================================
CRITICAL EXECUTION RESTRICTION
============================================================

DO NOT RUN ANY CODE.

DO NOT:
- compile SystemVerilog
- run simulations
- invoke Verilator
- invoke Icarus Verilog
- invoke cocotb
- invoke Python testbenches
- invoke Yosys
- invoke OpenROAD
- invoke Make
- invoke shell scripts
- install packages
- execute Docker
- execute any command requiring external tools

The required tools are NOT installed in my environment.

Your task in this conversation is ONLY to CREATE AND REVIEW THE COMPLETE CODEBASE.

You may reason through the code manually and perform static consistency checks mentally.

Never claim that a simulation, synthesis, timing analysis, P&R, or verification run passed.

Instead use statements such as:
"Static inspection indicates..."
"Expected behavior is..."
"Simulation should verify..."
"Not executed because tools are unavailable."

============================================================
PRIMARY ENGINEERING OBJECTIVE
============================================================

Design a hardware accelerator that computes:

                    C = A × B

where the matrix elements are INT8 values.

The baseline architecture should use an 8×8 systolic array.

The architecture should exploit:
1. Spatial parallelism
2. Data reuse
3. Local communication between PEs
4. Pipelined MAC operations
5. Streaming data transfer
6. Memory/computation overlap

The final design should expose AXI4-Stream interfaces and use ping-pong buffering to allow one buffer to be consumed by the compute engine while the other buffer is prepared/loaded.

============================================================
IMPORTANT ARCHITECTURAL DECISIONS
============================================================

Before writing the final RTL, reason carefully about the architecture.

Use a clear and defensible systolic dataflow.

Prefer OUTPUT-STATIONARY dataflow unless there is a strong technical reason to choose another architecture.

Explain internally, and document in the project:
- why the selected dataflow was chosen
- how A moves
- how B moves
- where partial sums reside
- when accumulation occurs
- how results are produced
- how many cycles are required
- how pipeline filling/draining works

Do NOT mix incompatible dataflow assumptions.

Create a precise cycle-level description of the systolic movement.

============================================================
ARITHMETIC SPECIFICATION
============================================================

Use signed INT8 operands.

Define explicitly:
- DATA_WIDTH = 8
- signed arithmetic
- accumulator width
- product width
- overflow behavior
- reset behavior

Prefer:

INT8 × INT8 → wider product → INT32 accumulator

unless a better implementation choice is technically justified.

Do not silently truncate intermediate values.

Be careful with SystemVerilog signed casting and multiplication.

All arithmetic must be explicitly signed where required.

Parameterize:

DATA_WIDTH
ACC_WIDTH
ARRAY_ROWS
ARRAY_COLS

Default:

ARRAY_ROWS = 8
ARRAY_COLS = 8
DATA_WIDTH = 8
ACC_WIDTH = 32

============================================================
PROCESSING ELEMENT
============================================================

Create a reusable PE module.

Each PE must conceptually perform:

    accumulator <= accumulator + (A × B)

The PE must also support systolic propagation of operands.

The PE should be synchronous and registered.

Define clean interfaces such as:
- clk
- reset
- enable/valid as appropriate
- A input
- B input
- accumulator/result
- propagated A output
- propagated B output

Do not create unnecessary ports.

Make the PE independently understandable and testable.

============================================================
SYSTOLIC ARRAY
============================================================

Create a parameterized systolic-array module.

Default:

8 rows
8 columns

Therefore:

8 × 8 = 64 PEs

Do NOT manually instantiate 64 individual PE instances.

Use SystemVerilog generate loops.

The architecture must support changing:

4×4
8×8
16×16

through parameters wherever practical.

The code must remain structurally scalable.

The word "scalable" must be justified by parameterization.

Document any limitations of scalability.

============================================================
MATRIX MULTIPLICATION
============================================================

Define the supported matrix operation clearly.

For the baseline, support tiled matrix multiplication where the systolic array computes a matrix tile.

Explain:
- matrix dimensions
- tile dimensions
- K dimension
- accumulation across K
- output dimensions
- handling of K > ARRAY_COLS or K > ARRAY_ROWS if applicable

Do not pretend that the 8×8 array automatically supports arbitrary matrix dimensions without tiling/control.

If the baseline supports 8×8 × 8×8 matrix multiplication, explicitly state that.

If larger matrices are supported through tiling, implement/document the tiling architecture.

============================================================
CONTROL FSM
============================================================

Create a dedicated controller.

The controller should coordinate:

IDLE
LOAD
COMPUTE
DRAIN
OUTPUT
DONE

or another technically correct state structure.

Define:
- start behavior
- busy behavior
- completion behavior
- reset behavior
- buffer selection
- systolic-array enable
- counters
- tile counters
- output sequencing

Use counters rather than hard-coded timing wherever possible.

Avoid magic numbers.

Create localparams/constants for important cycle counts.

============================================================
PING-PONG BUFFER
============================================================

Implement a genuine double-buffer architecture.

Use:

BUFFER_A
BUFFER_B

Conceptually:

While:
    Buffer A → compute

the system can:

    Buffer B ← next input data

Then swap.

Next phase:

    Buffer B → compute
    Buffer A ← next input data

The architecture should have:
- buffer select
- active buffer
- load buffer
- swap mechanism
- valid/full state as appropriate

Clearly separate:
1. memory/buffer logic
2. compute engine
3. controller

Do not claim that computation and loading are perfectly simultaneous unless the RTL architecture actually supports that concurrency.

Document the exact overlap behavior.

============================================================
AXI4-STREAM
============================================================

Implement AXI4-Stream interfaces for input and output.

At minimum consider:

Input:
- s_axis_tdata
- s_axis_tvalid
- s_axis_tready
- s_axis_tlast

Output:
- m_axis_tdata
- m_axis_tvalid
- m_axis_tready
- m_axis_tlast

Use correct AXI4-Stream handshake semantics:

TRANSFER occurs when:

    TVALID && TREADY

The producer must hold data stable while TVALID is asserted and TREADY is low, according to the protocol.

Implement proper backpressure handling.

Do not drop data if:
    TREADY = 0

Do not advance output state unless the transfer occurs.

Define what TLAST means in this accelerator:
- end of tile
- end of matrix
- or another clearly documented packet boundary

Choose one consistent convention.

============================================================
DATA PACKING
============================================================

Define how INT8 values are packed into AXI4-Stream TDATA.

For example:

TDATA_WIDTH = 64

could represent:

8 × INT8 values per transfer.

But choose the width based on a coherent architecture.

Parameterize:

AXIS_DATA_WIDTH

If using 64-bit AXI data:

64 bits = 8 INT8 elements

Document:
- byte ordering
- element ordering
- row/column mapping
- packing format
- unpacking logic

Avoid ambiguity.

============================================================
TOP-LEVEL MODULE
============================================================

Create one clean top-level accelerator module.

Example conceptual hierarchy:

accelerator_top
│
├── axi_stream_input
│
├── input_controller
│
├── ping_pong_buffer
│
├── systolic_controller
│
├── systolic_array
│     └── PE × 64
│
├── output_buffer
│
└── axi_stream_output

The exact hierarchy may differ if you have a better architecture.

Keep modules modular.

============================================================
VERIFICATION ARCHITECTURE
============================================================

Create a serious verification environment.

Do NOT only create one basic testbench.

Provide:

1. PE testbench
2. Systolic-array testbench
3. AXI-stream testbench
4. Ping-pong-buffer testbench if useful
5. Full accelerator integration testbench

Test:
- positive values
- negative values
- zero
- maximum INT8
- minimum INT8
- mixed-sign multiplication
- accumulation
- random matrices
- identity matrices
- zero matrices
- all-ones matrices
- negative matrices
- corner cases
- reset
- backpressure
- TLAST
- multiple transactions
- buffer switching

============================================================
PYTHON GOLDEN MODEL
============================================================

Create a Python reference model.

Use NumPy where appropriate.

The Python model should calculate:

C = A @ B

using appropriate integer arithmetic and explicitly defined accumulator behavior.

Create utilities to:
- generate random INT8 matrices
- generate corner-case matrices
- calculate expected outputs
- compare RTL output against expected output

Do not execute the Python scripts.

Only write them.

Clearly separate:
- reference model
- test vector generation
- result checking

============================================================
SYSTEMVERILOG ASSERTIONS
============================================================

Add useful SystemVerilog Assertions where appropriate.

Examples:

- AXI data stability when TVALID && !TREADY
- no illegal state transitions
- reset behavior
- valid/ready protocol assumptions
- output only changes on valid transfer where appropriate
- buffer ownership correctness
- counter bounds

Do not add meaningless assertions.

Each assertion should test a real design invariant.

============================================================
COVERAGE
============================================================

Provide a verification coverage strategy.

If practical, create SystemVerilog covergroups for:
- positive operands
- negative operands
- zero
- maximum/minimum values
- AXI backpressure
- TLAST
- buffer swap
- different transaction sizes
- reset
- compute states

Do not claim coverage percentages because nothing is executed.

============================================================
REFERENCE TEST CASES
============================================================

Include explicit small matrix examples.

For example:

A =
[1 2
 3 4]

B =
[5 6
 7 8]

Expected:

C =
[19 22
 43 50]

Also include an 8×8 deterministic test case.

Create expected values carefully.

Do the arithmetic manually/reason through it before placing expected values in files.

Avoid incorrect golden data.

============================================================
FILE STRUCTURE
============================================================

Create a professional project structure.

Use something similar to:

project_root/
│
├── rtl/
│   ├── pe/
│   │   └── systolic_pe.sv
│   │
│   ├── array/
│   │   └── systolic_array.sv
│   │
│   ├── memory/
│   │   ├── ping_pong_buffer.sv
│   │   └── buffer_controller.sv
│   │
│   ├── axi/
│   │   ├── axi_stream_input.sv
│   │   └── axi_stream_output.sv
│   │
│   ├── control/
│   │   └── accelerator_controller.sv
│   │
│   └── top/
│       └── systolic_accelerator_top.sv
│
├── tb/
│   ├── pe_tb.sv
│   ├── systolic_array_tb.sv
│   ├── axi_stream_tb.sv
│   ├── ping_pong_tb.sv
│   └── accelerator_tb.sv
│
├── verification/
│   ├── assertions/
│   ├── coverage/
│   └── test_vectors/
│
├── python/
│   ├── golden_model.py
│   ├── generate_vectors.py
│   └── compare_results.py
│
├── scripts/
│   ├── compile.f
│   ├── simulation.f
│   └── synthesis.tcl
│
├── constraints/
│   └── timing.sdc
│
├── docs/
│   ├── architecture.md
│   ├── dataflow.md
│   ├── verification.md
│   ├── memory_architecture.md
│   ├── axi_protocol.md
│   └── performance.md
│
├── synthesis/
│
├── openroad/
│
├── README.md
│
└── LICENSE

You may modify this structure if necessary.

============================================================
CODING STYLE
============================================================

Use professional SystemVerilog.

Rules:

- Use logic rather than reg/wire where appropriate.
- Use always_ff for sequential logic.
- Use always_comb for combinational logic.
- Use typedef enum logic for FSM states.
- Avoid inferred latches.
- Avoid unnecessary asynchronous combinational paths.
- Use parameters/localparams.
- Avoid magic numbers.
- Use meaningful signal names.
- Keep modules single-purpose.
- Add concise comments explaining WHY, not obvious syntax.
- Avoid excessive comments.
- Keep reset strategy consistent across the design.
- Avoid multiple drivers.
- Avoid implicit signed/unsigned conversions.
- Explicitly cast arithmetic where required.
- Avoid unsynthesizable constructs in synthesizable RTL.

============================================================
RESET STRATEGY
============================================================

Choose one reset strategy and use it consistently.

Prefer a synchronous active-high reset unless there is a strong architectural reason otherwise.

Document:
- reset effect on FSM
- reset effect on buffers
- reset effect on accumulators
- reset effect on AXI signals
- reset effect on PE pipeline state

Ensure no stale accumulation survives reset.

============================================================
PARAMETERIZATION
============================================================

Parameterize where meaningful:

ARRAY_ROWS
ARRAY_COLS
DATA_WIDTH
ACC_WIDTH
AXIS_DATA_WIDTH
K_DIM or equivalent
BUFFER_DEPTH

Do not over-parameterize everything.

The default configuration must remain:

8×8
INT8
32-bit accumulation

============================================================
PERFORMANCE MODEL
============================================================

Before finalizing the architecture, derive the expected cycle behavior.

Document:
- pipeline fill latency
- steady-state throughput
- drain latency
- total cycles for an 8×8 tile
- effect of K dimension
- effect of stalls
- effect of buffer loading
- expected PE utilization

Do not invent measured performance.

Clearly distinguish:

THEORETICAL PEAK
EXPECTED RTL BEHAVIOR
ACTUAL MEASURED RESULT

Since execution is prohibited, only the first two can be provided.

============================================================
RESOURCE ESTIMATION
============================================================

Provide a theoretical resource discussion.

For 8×8:

64 PEs

Each PE approximately contains:
- multiplier
- accumulator
- registers
- forwarding registers

Discuss:
- multiplier count
- accumulator count
- register count conceptually
- buffer memory
- AXI logic
- controller logic

Do NOT claim actual LUT/FF/BRAM/gate counts without synthesis.

============================================================
ASIC READINESS
============================================================

Make the RTL synthesis-friendly.

Prepare scripts/configuration for:
- Yosys
- SKY130 if appropriate
- OpenROAD

But DO NOT execute them.

Provide:
- synthesis script
- top module definition
- clock definition
- SDC constraints
- basic timing assumptions

Use placeholders where actual PDK installation paths would be machine-specific.

Do not hard-code nonexistent paths.

============================================================
FPGA/ASIC SEPARATION
============================================================

Keep the core accelerator independent of a specific FPGA vendor.

Do not use Xilinx-specific primitives unless explicitly placed in an optional wrapper.

The core should remain portable SystemVerilog.

If useful, create:

rtl/
    core/

platform/
    generic/
    xilinx_optional/

But do not make Xilinx IP mandatory.

============================================================
DOCUMENTATION
============================================================

Create a professional README.md.

It must contain:

1. Project overview
2. Real-world problem
3. Proposed solution
4. Why systolic architecture
5. Why INT8
6. Why 8×8
7. Why AXI4-Stream
8. Why ping-pong buffering
9. Architecture diagram
10. PE architecture
11. Dataflow explanation
12. Timing/cycle explanation
13. Memory architecture
14. AXI protocol
15. Verification methodology
16. File structure
17. Toolchain
18. Expected outputs
19. Synthesis methodology
20. Performance metrics to measure
21. Known limitations
22. Future improvements

============================================================
ARCHITECTURE DIAGRAMS
============================================================

Create diagrams in Markdown/ASCII where appropriate.

At minimum provide diagrams for:

1. Overall accelerator
2. Single PE
3. 8×8 systolic array
4. Data movement
5. Ping-pong buffering
6. AXI4-Stream interface
7. Controller FSM
8. Verification flow
9. RTL-to-GDS flow

Make diagrams technically consistent with the actual RTL.

Do not create diagrams that describe behavior the code does not implement.

============================================================
DEVELOPMENT PHASES
============================================================

Structure the project into these implementation phases:

PHASE 1:
Single INT8 MAC PE

PHASE 2:
PE verification

PHASE 3:
2×2 systolic array

PHASE 4:
2×2 matrix-multiplication verification

PHASE 5:
Parameterized systolic array

PHASE 6:
4×4 validation

PHASE 7:
8×8 / 64-PE architecture

PHASE 8:
Controller/FSM

PHASE 9:
Ping-pong buffers

PHASE 10:
AXI4-Stream

PHASE 11:
Full integration

PHASE 12:
Python golden model integration

PHASE 13:
Assertions and coverage

PHASE 14:
Synthesis scripts

PHASE 15:
OpenROAD preparation

PHASE 16:
Performance/resource analysis

============================================================
VERY IMPORTANT SELF-REVIEW PROCESS
============================================================

After creating each major phase, STOP and review your own work.

Do not blindly continue.

For every phase, perform a STATIC DESIGN REVIEW.

Check:

1. Interface consistency
2. Signal widths
3. Signedness
4. Clock/reset consistency
5. FSM transitions
6. Counter widths
7. Array indexing
8. Generate-loop correctness
9. Dataflow consistency
10. Pipeline timing
11. Buffer ownership
12. AXI handshake correctness
13. TLAST behavior
14. Potential deadlocks
15. Potential data loss
16. Potential overflow
17. Potential stale data
18. Multiple-driver issues
19. Inferred latch risks
20. Synthesizability
21. Testbench compatibility
22. Top-level port consistency
23. File dependencies
24. Parameter propagation
25. Documentation/code consistency

============================================================
FINAL FULL CODEBASE AUDIT
============================================================

After writing ALL files, perform a second complete review from scratch.

Pretend you are an external senior RTL reviewer receiving this repository.

Check the complete project as one integrated system.

Specifically answer:

A. Does every referenced module exist?

B. Does every module port match its instantiation?

C. Are widths consistent?

D. Are all arithmetic operations correctly signed?

E. Are PE connections consistent with the chosen dataflow?

F. Is the systolic timing internally consistent?

G. Does the controller generate the correct number of cycles?

H. Can the controller deadlock?

I. Can AXI backpressure cause data loss?

J. Does TLAST correspond to the documented transaction boundary?

K. Can the output buffer overwrite unread data?

L. Can the two ping-pong buffers accidentally become active simultaneously?

M. Is the buffer swap safe?

N. Can stale accumulator values contaminate a new matrix multiplication?

O. Are reset values sufficient?

P. Is the array truly parameterized?

Q. Is 8×8 the default configuration?

R. Can 4×4 theoretically elaborate?

S. Can 16×16 theoretically elaborate?

T. Is the design synthesizable?

U. Are testbenches compatible with the RTL interfaces?

V. Does the Python golden model match the RTL arithmetic assumptions?

W. Are expected test vectors mathematically correct?

X. Are the synthesis scripts consistent with the actual top-level?

Y. Are timing constraints consistent?

Z. Are the README and architecture diagrams consistent with the code?

============================================================
BUG CLASSIFICATION
============================================================

During your review, classify issues as:

CRITICAL
- prevents compilation/elaboration
- incorrect functional behavior
- protocol violation
- incorrect dataflow
- architectural deadlock

HIGH
- likely functional issue
- incorrect parameterization
- incorrect arithmetic
- serious verification gap

MEDIUM
- maintainability issue
- weak verification
- documentation mismatch
- non-optimal architecture

LOW
- naming/style
- optional cleanup
- documentation improvement

Fix CRITICAL and HIGH issues before declaring the codebase complete.

Do not merely mention a bug and continue.

Correct the code.

Then re-review the corrected code.

============================================================
NO FALSE COMPLETION CLAIM
============================================================

At the end, provide three statuses:

1. STATIC CODE STATUS
2. EXECUTION STATUS
3. REMAINING VALIDATION

Example:

STATIC CODE STATUS:
"Codebase has been statically reviewed and internally cross-checked."

EXECUTION STATUS:
"Not executed. Required tools are unavailable and execution was explicitly prohibited."

REMAINING VALIDATION:
"Simulation, synthesis, STA, and physical implementation must be run later in the user's environment."

Never say:
"Simulation passed"
"Synthesis passed"
"Timing passed"
"RTL is verified"

unless those things have actually been executed.

============================================================
OUTPUT FORMAT
============================================================

Work systematically.

Do NOT dump everything randomly.

Follow this order:

STEP 1:
Architecture decisions

STEP 2:
Complete project tree

STEP 3:
Core RTL

STEP 4:
Verification testbenches

STEP 5:
Python golden model

STEP 6:
Assertions/coverage

STEP 7:
Scripts/constraints

STEP 8:
Documentation

STEP 9:
Interview material

STEP 10:
Resume bullets

STEP 11:
Static review after each major section

STEP 12:
Final full-codebase audit

STEP 13:
List any remaining issues

If the response becomes too long for one message, continue in logically numbered parts.

DO NOT omit files simply because the response is long.

When creating each file, clearly label it:

FILE:
rtl/pe/systolic_pe.sv

Then provide the complete contents.

Do not provide pseudocode when actual code is requested.

Do not use "...", "rest of code", "same as above", or placeholders inside the source files.

Every required source file must contain complete code.

============================================================
QUALITY STANDARD
============================================================

Treat this as if the repository will be reviewed by a senior RTL engineer.

Prioritize:

CORRECTNESS
over
COMPLEXITY

Prefer a clean, understandable architecture over unnecessary sophistication.

Do not add features merely to make the project sound impressive.

Every feature must have a real architectural purpose.

The final project should be understandable by an engineering student but technically strong enough to discuss in a VLSI interview.

============================================================
FINAL REQUIREMENT
============================================================

Do not stop after writing the first version.

The workflow must be:

DESIGN
↓
IMPLEMENT
↓
STATIC REVIEW
↓
FIX
↓
STATIC REVIEW AGAIN
↓
INTEGRATE
↓
FULL SYSTEM REVIEW
↓
FIX
↓
FINAL AUDIT
↓
FINAL CODEBASE

The final response must clearly identify:
- what was created
- what was statically checked
- what was corrected
- what has NOT been executed
- what I need to run later when tools are installed
- what results I should collect from simulation/synthesis
- what performance numbers should eventually be reported

Remember:

DO NOT EXECUTE ANY CODE.
ONLY CREATE, INSPECT, CROSS-CHECK, CORRECT, AND DOCUMENT THE COMPLETE CODEBASE.